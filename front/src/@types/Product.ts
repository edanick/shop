export type Product = {
    _id?: string,
    title: string,
    image?: string,
    description: string,
    currency: string,
    price: number,
    shipping: number,
    color: string,
    condition: string,
    stock: number
}
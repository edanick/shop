export type Product = {
    id?: number,
    title: string,
    description: string,
    currency: string,
    price: number,
    shipping: number,
    color: string,
    condition: string,
    stock: number
}